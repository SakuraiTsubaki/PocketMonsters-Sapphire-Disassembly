name: validate

on:
  push:
  pull_request:

jobs:
  repository-safety:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject ROM binaries
        run: make check-no-rom
      - name: Validate Python tools
        run: python3 -m py_compile tools/*.py
